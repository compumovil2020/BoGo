<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bogo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyRedeemActivity"></activity>
        <activity android:name=".PlaceList"></activity>
        <activity android:name=".AddReviewActivity"></activity>
        <activity android:name=".SeeReview" ></activity>
        <activity android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".SearchActivity"></activity>
        <activity android:name=".RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".PlaceMapActivity"></activity>
        <activity android:name=".PlaceDescriptionActivity"></activity>
        <activity android:name=".BienvenidoActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DropMenuActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>